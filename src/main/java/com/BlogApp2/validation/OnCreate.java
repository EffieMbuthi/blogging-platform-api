package com.BlogApp2.validation;

/**
 * Bean Validation group marker for constraints that must only run when a new
 * record is being created, not when an existing one is being updated.
 */
public interface OnCreate {
}
